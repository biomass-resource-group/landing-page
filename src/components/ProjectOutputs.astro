---
interface Line {
  title: string;
  description: string;
}

interface Props {
  lines: readonly Line[];
  caveat?: string;
}

const { lines, caveat } = Astro.props;
---

<div class="project-outputs" data-reveal>
  <div class="project-outputs__grid">
    {
      lines.map((line, index) => (
        <article class="project-outputs__item">
          <span>{String(index + 1).padStart(2, '0')}</span>
          <h3>{line.title}</h3>
          <p>{line.description}</p>
        </article>
      ))
    }
  </div>
  {caveat && <p class="project-outputs__note">{caveat}</p>}
</div>
